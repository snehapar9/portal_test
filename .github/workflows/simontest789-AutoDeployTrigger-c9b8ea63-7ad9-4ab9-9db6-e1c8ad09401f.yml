name: Trigger auto deployment for simontest789

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/simontest789-AutoDeployTrigger-c9b8ea63-7ad9-4ab9-9db6-e1c8ad09401f.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SIMONTEST789_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SIMONTEST789_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SIMONTEST789_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.SIMONTEST789_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SIMONTEST789_REGISTRY_PASSWORD }}
          containerAppName: simontest789
          resourceGroup: simonj
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


